# StudySphere

The app lives in [`studysphere/`](./studysphere).

```bash
cd studysphere
npm install
npx prisma generate
npx prisma db push
npx prisma db seed
npm run dev
```

Then open http://localhost:3000
